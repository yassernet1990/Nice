const menu=document.querySelector('.menu'),openBtn=document.querySelector('.menu-btn'),closeBtn=document.querySelector('.menu-close');openBtn?.addEventListener('click',()=>menu.classList.add('open'));closeBtn?.addEventListener('click',()=>menu.classList.remove('open'));menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));document.querySelectorAll('.faq-item').forEach(i=>i.querySelector('.q').addEventListener('click',()=>i.classList.toggle('open')));const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in')),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));const cursor=document.querySelector('.cursor');window.addEventListener('mousemove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});document.querySelectorAll('[data-count]').forEach(el=>{let done=false;const ob=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting&&!done){done=true;let end=+el.dataset.count,n=0;let t=setInterval(()=>{n+=Math.max(1,Math.ceil(end/50));if(n>=end){n=end;clearInterval(t)}el.textContent=n+(el.dataset.suffix||'')},28)}}));ob.observe(el)});